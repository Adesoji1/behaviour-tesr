# Behavioural anti-fraud ML subsystem — PINNED dependencies.
#
# These are the SINGLE SOURCE OF TRUTH for both training (Dockerfile.ml) and serving
# (Dockerfile.api). Both images run `pip install -r ml/requirements-ml.txt`, so the model is
# trained and served with the EXACT same library versions — no unpickling / version skew.
# The versions below match the pytorch/pytorch:2.3.1-cuda12.1-cudnn8-runtime base image, so
# `torch`/`numpy` are already satisfied by the base and are not re-installed.
#
# If you bump any version here, rebuild BOTH images and RETRAIN so the pickles stay in sync.

# --- core (tree + graph-feature path; CPU only) ---
numpy==1.26.4                 # matches the torch 2.3.1 base (torch 2.3.x is NOT numpy-2 compatible)
pandas==2.3.3
scikit-learn==1.7.2           # pinned: IsolationForest/StandardScaler pickles must load identically
scipy==1.15.3
matplotlib==3.10.9
seaborn==0.13.2
joblib==1.5.3
psycopg[binary]==3.3.4
pyarrow==25.0.0

# --- deep learning (Autoencoder + GNN) ---
# torch is provided by the base image (torch==2.3.1+cu121). On a bare host install the matching
# CUDA build first:  pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cu121
torch==2.3.1
torch-geometric==2.8.0.post1

# --- serving (FastAPI inference API — ml.api) ---
fastapi==0.141.1
uvicorn[standard]==0.52.0
httpx==0.28.1                 # webhook delivery to Adhere
